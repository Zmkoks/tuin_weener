/**
 * Gewone formuleringen die naar een bestaande vakterm verwijzen.
 * Nieuwe vaktermen worden op hun eigen naam automatisch herkend; voeg hier alleen
 * afwijkende, veelgebruikte benamingen toe, één keer per vakterm.
 */
export const vaktermAliassen: Record<string, string[]> = {
  'groen-hart': ['hart van de plant'],
  'kaal-hout': ['kaal oud hout'],
};
