'use client';
import { FormEvent, useState } from 'react';
export default function Login() { const [code, setCode] = useState(''); const [fout, setFout] = useState(''); const [bezig, setBezig] = useState(false); const [zichtbaar, setZichtbaar] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setBezig(true); setFout(''); const r = await fetch('/api/auth/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ code }) }); if (r.ok) window.location.reload(); else { setFout('Inloggen lukt niet.'); setCode(''); setBezig(false); } } return <div className="beheer-login"><p className="eyebrow">VOOR MEDEWERKERS</p><h1>Beheren</h1><p className="lead">Log in om de planten en plekken van de tuin aan te passen.</p><form onSubmit={submit}><label htmlFor="beheer-code">Beheercode</label><div className="beheer-code-veld"><input id="beheer-code" type={zichtbaar ? 'text' : 'password'} autoComplete="current-password" value={code} onChange={e => setCode(e.target.value)} autoFocus/><button type="button" className="beheer-zichtbaar" onClick={() => setZichtbaar(v => !v)}>{zichtbaar ? 'Verberg code' : 'Maak code zichtbaar'}</button></div><button className="beheer-doen" disabled={!code || bezig}>{bezig ? 'Even wachten…' : 'Inloggen'}</button>{fout && <p className="beheer-fout" role="alert">{fout}</p>}</form></div>; }
