import type { Metadata } from 'next';
import Scherm from './Scherm';

export const metadata: Metadata = { title: 'Waar stond de plant? · Beheren' };

export default function Pagina() {
  return <Scherm />;
}
